# stale-fixed-rendition

Mapping claim treats stale fixed output as usable.

Corpus 0.1.1 attribution: SPD-STATE-007, because Mapping requires current fixed output. The package provides no processor-behavior evidence for SPD-STATE-001. Package bytes are unchanged.

Package: `document.epub`. Expected result: `expected.json`.
