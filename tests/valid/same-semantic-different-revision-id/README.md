# same-semantic-different-revision-id

Same authoritative bytes as minimal-base, committed under a different opaque Revision ID with the same semanticStateDigest.

Package: `document.epub`. Expected result: `expected.json`.
