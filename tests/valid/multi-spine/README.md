# multi-spine

Two ordered XHTML spine items.

Corpus 0.1.1: chapter1.xhtml uses n_article01; chapter2.xhtml uses n_article02. All affected resource, projection, inventory and lifecycle descriptor digests were recomputed. This remains a genuinely valid multi-spine case. The historical cross-spine duplicate is retained as invalid/cross-spine-duplicate-node-id.

Package: `document.epub`. Expected result: `expected.json`.
